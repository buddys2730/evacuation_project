name: HazardPolygonRenderer設計mdとprops完全一致チェック

on:
  push:
    paths:
      - 'docs/HazardPolygonRenderer.md'
      - 'src/components/map/HazardPolygonRenderer.js'
  pull_request:
    paths:
      - 'docs/HazardPolygonRenderer.md'
      - 'src/components/map/HazardPolygonRenderer.js'

jobs:
  check-hazardpolygonrenderer-props:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Check HazardPolygonRenderer props一致
        run: |
          cd frontend
          npm install
          npm run check:hazardpolygonrenderer
