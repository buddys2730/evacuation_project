name: CI Checks

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Debug: check files
        run: ls -la

      - name: Install dependencies
        run: npm ci
        working-directory: ./  # ✅ 正しい位置（runの「下」ではなく「横」）

      - name: Run ESLint
        run: npx eslint .
        working-directory: ./  # ✅ 正しい位置

      - name: Run Prettier check
        run: npx prettier --check .
        working-directory: ./  # ✅ 正しい位置

      - name: Run Markdown validation
        run: node scripts/check-md-rules.js || echo "Markdown rule check failed"
        working-directory: ./  # ✅ 正しい位置

      - name: Markdown契約・設計チェック
        run: node scripts/validate-md.js || echo "Markdown design check failed"
        working-directory: ./  # ✅ 正しい位置
